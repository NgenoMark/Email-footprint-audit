# Frontend

Next.js (React + TypeScript) UI for Email Footprint Audit.

## Dev

```
npm install
npm run dev
```

## Env

Create `frontend/.env.local`:

```
NEXT_PUBLIC_API_URL=http://localhost:8000/api
```
